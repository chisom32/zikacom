export default function AdminDashboard() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-[#003580] mb-2">Dashboard</h1>
      <p className="text-gray-500 text-sm">
        Overview coming next — pending applications, listing counts, etc.
      </p>
    </div>
  );
}